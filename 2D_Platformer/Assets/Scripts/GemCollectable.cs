using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GemCollectable : CollectableProperty
{
    // Start is called before the first frame update
    void Start()
    {
        Points = 5;
    }

    // Update is called once per frame
    void Update()
    {

    }
}
